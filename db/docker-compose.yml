version: '3.7'

services:
  db_transport_app:
    image: postgres
    container_name: db_transport_app
    environment:
      POSTGRES_PASSWORD: 123456
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
